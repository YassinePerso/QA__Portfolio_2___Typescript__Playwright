# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Sprint_1___Access-management/automatisation/tests/logout.spec.ts >> Logout >> TC-025 - Session détruite après logout
- Location: Sprint_1___Access-management/automatisation/tests/logout.spec.ts:58:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected: "https://with-bugs.practicesoftwaretesting.com/#/auth/login"
Received: "https://with-bugs.practicesoftwaretesting.com/#/account"
Timeout:  5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × locator resolved to <html dir="ltr" lang="en-US">…</html>
       - unexpected value "https://with-bugs.practicesoftwaretesting.com/#/account"

```

```yaml
- main:
  - img "Icon for with-bugs.practicesoftwaretesting.com"
  - heading "with-bugs.practicesoftwaretesting.com" [level=1]
  - heading "Performing security verification" [level=2]
  - paragraph: This website uses a security service to protect against malicious bots. This page is displayed while the website verifies you are not a bot.
- contentinfo:
  - text: "Ray ID:"
  - code: a2a266098aedc93b
  - text: Performance and Security by
  - link "Cloudflare, opens in a new tab":
    - /url: https://www.cloudflare.com?utm_source=challenge&utm_campaign=m
    - text: Cloudflare
  - link "Privacy, opens in a new tab":
    - /url: https://www.cloudflare.com/privacypolicy/
    - text: Privacy
```

# Test source

```ts
  1  | import { test, expect, chromium } from '@playwright/test';
  2  | import { LoginPage } from '../pages/login.page';
  3  | import { NavbarPage } from '../pages/navbar.page';
  4  | import { RegisterPage } from '../pages/register.page';
  5  | import { TEST_USER, URLS } from '../fixtures/test-data';
  6  | 
  7  | test.describe('Logout', () => {
  8  | 
  9  |   let loginPage: LoginPage;
  10 |   let navbarPage: NavbarPage;
  11 | 
  12 |   test.beforeAll(async () => {
  13 |     const browser = await chromium.launch();
  14 |     const context = await browser.newContext();
  15 |     const page = await context.newPage();
  16 |     const register = new RegisterPage(page);
  17 | 
  18 |     await register.navigate(URLS.register);
  19 |     await register.waitForPageLoad();
  20 |     await register.fillForm(TEST_USER);
  21 | 
  22 |     try {
  23 |       await page.waitForURL(`**${URLS.login}`, { timeout: 15000 });
  24 |       await page.waitForTimeout(2000); // Attendre que le compte soit bien enregistré
  25 |     } catch {
  26 |       console.log('Compte déjà existant > on continue');
  27 |     }
  28 | 
  29 |     await browser.close();
  30 |   });
  31 | 
  32 |   // beforeEach pour se connecter avant chaque test de logout
  33 |   test.beforeEach(async ({ page }) => {
  34 |     loginPage = new LoginPage(page);
  35 |     navbarPage = new NavbarPage(page);
  36 |     await loginPage.navigate(URLS.login);
  37 |     await loginPage.waitForPageLoad();
  38 |     await loginPage.login(TEST_USER.email, TEST_USER.password);
  39 |     // Timeout augmenté (20s) -> l'environnement CI est plus lent que le poste
  40 |     
  41 |     await page.waitForURL(`**${URLS.account}`, { timeout: 20000 });
  42 |     await page.waitForLoadState('domcontentloaded');
  43 |   });
  44 | 
  45 |   // TC-023 - Vérifier la présence du bouton de déconnexion
  46 |   test('TC-023 - Vérifier la présence du bouton de déconnexion', { tag: '@smoke' }, async () => {
  47 |     await navbarPage.dropdownButton.click();
  48 |     await expect(navbarPage.logoutButton).toBeVisible();
  49 |   });
  50 | 
  51 |   // TC-024 - Logout - redirection vers /auth/login
  52 |   // test('TC-024 - Logout - redirection vers /auth/login', { tag: '@smoke' }, async () => {
  53 |   //   await navbarPage.logout();
  54 |   //   await expect(navbarPage.page).toHaveURL(/auth\/login/, { timeout: 20000 });
  55 |   // });
  56 | 
  57 |   // TC-025 - Session détruite après logout
  58 |   test('TC-025 - Session détruite après logout', { tag: '@regression' }, async ({ page }) => {
  59 |     await navbarPage.logout();
  60 |     await page.goto(URLS.account);
> 61 |     await expect(page).toHaveURL(URLS.login);
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  62 |   });
  63 | 
  64 |   // TC-026 - Accès /account après logout
  65 |   test('TC-026 - Accès /account après logout', { tag: '@regression' }, async () => {
  66 |     await navbarPage.logout();
  67 |     await navbarPage.page.goto(URLS.account);
  68 |     await expect(navbarPage.page).toHaveURL(/auth\/login/);
  69 |   });
  70 | 
  71 | });
```